#! /usr/bin/env python

import os
import sys
import time
from datetime import datetime, timedelta

mics = int(sys.argv[1])
os.environ['TZ'] = 'US/Central'
time.tzset()
d = datetime.fromtimestamp(0)
t = timedelta(microseconds=mics)
print((d + t).ctime())
